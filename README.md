# Workers Management

## Tech Stack

* React
* Redux
* SCSS
* Webpack bundler
* npm package manager

## Steps To setup
1. `npm install`
2. `npm run start:dev`
3. `npm run webpack` in another terminal
